name = "lorstar0121"
main = "worker.js"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# ── KV 命名空间：和 Cloudflare Pages 项目里绑定的必须是同一个 CHAT_BACKUPS ──
# 运行 `wrangler kv namespace create CHAT_BACKUPS` 拿到 id 后填进来，
# 注意：这个 id 要和 Pages 项目 Settings → Functions → KV bindings 里绑的是同一个命名空间，
# 这样 Worker 和 Pages Functions 才能读到同一份数据。
[[kv_namespaces]]
binding = "CHAT_BACKUPS"
id = "REPLACE_WITH_YOUR_KV_ID"
preview_id = "REPLACE_WITH_YOUR_KV_PREVIEW_ID"

# ── Cron 触发器：每5分钟跑一次，和原来 netlify.toml 里的 scheduled-proactive 频率一致 ──
[[triggers]]
crons = ["*/5 * * * *"]

# ── Secrets（部署后用 `wrangler secret put <名字>` 逐个设置，不要写在这个文件里）──
# wrangler secret put ONESIGNAL_APP_ID
# wrangler secret put ONESIGNAL_REST_API_KEY
#
# ── 普通变量（不是密钥，可以直接写在这里，取消注释即可）──
# [vars]
# SITE_URL = "https://lorstar0121.moraleskento8.workers.dev/"
