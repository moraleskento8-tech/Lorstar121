name        = "lorstar0121"
main        = "worker.js"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# ── KV 命名空间（存储 Push 订阅信息）──
[[kv_namespaces]]
binding  = "PUSH_SUBS"
id       = "REPLACE_WITH_YOUR_KV_ID"        # 运行 `wrangler kv:namespace create PUSH_SUBS` 获取
preview_id = "REPLACE_WITH_YOUR_KV_PREVIEW_ID"

# ── Cron 触发器（可选）──
[[triggers]]
crons = ["0 * * * *"]   # 每小时触发一次 scheduled()

# ── Secrets（通过 wrangler secret put 设置，不要写在这里）──
# VAPID_PUBLIC   = "Loreille0110"
# VAPID_PRIVATE  = "Loreille0110"
# VAPID_SUBJECT  = "mailto:moraleskento8@gmail.com"
# ONESIGNAL_API_KEY    = "os_v2_app_vugcqac5gfhppeqzeprbjloxoe4h2oevde3etsfwitiy5epvvdp22y5jeifo4ywa66gxgf4f3u5qgta2pzpdabdtb6ggejiird4h3ii"
